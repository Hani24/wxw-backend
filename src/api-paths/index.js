
module.exports = (App, params={})=>{

  // App.app_root;
  // App.public_html;
  // App.services_root;
  // App.logs_root;
  // App.dicts_root;

  return {
    // 'access-recovery-verify': '/public/user/access-recovery/verify/code/{0}',
  };

}
